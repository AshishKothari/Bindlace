# Bindlace
Bindlace — language- and driver-neutral web UI automation. Author portable JSON/YAML (*.wflow, *.wloc, optional *.wdata), merge to a resolved IR, and run with Playwright or Selenium via the bindlace CLI. Same specs, swappable runners.
