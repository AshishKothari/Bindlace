# Optional test data: referenced from flow as ${data.path}.
wflowSpecVersion: 1
kind: wdata
meta:
  id: local-env
data:
  baseUrl: https://localhost:3000
  user:
    email: user@example.com
    password: "use-env-in-ci"
